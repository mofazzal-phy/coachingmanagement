# Reasonix project memory

Notes the user pinned via the `#` prompt prefix. The whole file is
loaded into the immutable system prefix every session — keep it terse.

- PHASE 9 — REPORTING & ANALYTICS## GoalsBuild ERP-level reporting system.---## REPORT TYPES* revenue report* occupancy report* enrollment report* teacher performance* batch performance* course performance---## REPORT FEATURES* filters* charts* export* PDF* CSV* Excel* printable reports
- PHASE 11 — PERFORMANCE OPTIMIZATION## GoalsOptimize:* eager loading* indexes* caching* API performance* frontend lazy loading* query optimizationAvoid:* N+1 queries
- PHASE 11 — PERFORMANCE OPTIMIZATION## GoalsOptimize:* eager loading* indexes* caching* API performance* frontend lazy loading* query optimizationAvoid:* N+1 queries
